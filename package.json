{
  "name": "ats-optimizer-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build:backend": "npm run build --workspace=backend",
    "start:backend": "npm start --workspace=backend"
  },
  "engines": {
    "node": "18.x"
  }
}