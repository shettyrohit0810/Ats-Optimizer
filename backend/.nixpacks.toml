[phases.setup]
nixPkgs = ['nodejs_18']
cmds = ['npm install -g npm@9']

[phases.install]
cmds = ['cd backend && npm install --legacy-peer-deps']

[phases.build]
cmds = ['cd backend && npm run build']

[start]
cmd = 'cd backend && npm start'